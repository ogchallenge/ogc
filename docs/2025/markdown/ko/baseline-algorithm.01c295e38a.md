# 알고리즘 개발 방법 및 baseline 알고리즘 설명

*Update history:*

- *2025/07/19 - baseline_20250719 updated (bugs in heuristics fixed)*
- *2025/07/05 - baseline_20250705 updated (bugs in util.py fixed)*
- *2025/06/01 - baseline_20250601 updated (bugs in heuristics fixed)*
- *2025/05/31 - baseline_20250531 updated (minor bugs fixed)*
- *2025/05/19 - baseline_20250519 updated (minor bugs fixed)*
- *2025/05/07 - initial page created*

본 페이지에서는 경연에 참가하기 위해 알고리즘 코드를 작성하는 방법을 예제로 제공되는 baseline 알고리즘을 통해 설명합니다. 

## 1. 환경설정

알고리즘을 제출하기 전에 먼저 참가팀은 개인의 PC에서 알고리즘을 구현하고 알고리즘이 작동하는지 확인해야 합니다. 이를 위해 개인 PC에 알고리즘 평가용 서버와 같은 Python 환경을 만드는 과정을 설명합니다. 

참가팀이 사용 가능한 Python 패키지의 목록은 다음과 같습니다. 이들 패키지를 개별로 하나씩 설치할 필요 없이 아래의 conda 환경 생성을 이용하면 간단하게 평가 서버와 같은 환경을 만들 수 있습니다.

```
- python 3.11
- jupyterlab 4.4.1
- notebook 7.4.1
- ipympl 0.9.7
- matplotlib 3.8.4
- pandas 2.2.3
- networkx 3.4.2
- scipy 1.15.2
- scikit-learn 1.6.1
- numba 0.61.0
- cython 3.0.10
- jsbeautifier
- ortools 9.11.4210
- gurobipy 12.0.1
- xpress 9.5.4
- gymnasium 1.1.1
- torch 2.6.0
- torchvision 0.21.0
- tensorflow 2.18.0
- keras 3.9.0    
```


> 💡 알고리즘 구현에 필요한 패키지가 있으면 Slack 채널에서 요청하시면 추가할 수 있습니다!

### Conda 환경 만들기

Conda는 가상환경을 만들어 주는 프로그램입니다. 아나콘다([Anaconda](https://www.anaconda.com/download)) 또는 [miniforge](https://github.com/conda-forge/miniforge)등을 통해 설치할 수 있습니다. **본 경연 참가를 위해서는 miniforge를 추천합니다.**

> 💡 Conda가 무엇이고 왜 필요한지는 [여기](https://wikidocs.net/135082)를 참조하세요!

Conda 환경 생성 절차

[ogc2025_env_20250506.yml.zip](baselines/ogc2025_env_20250506.yml.zip)

1. 위 파일을 다운로드해서 임의의 폴더에 저장합니다. 
2. 저장한 폴더로 이동합니다.
3. Terminal 또는 cmd로 다음을 실행합니다. (*주의: `ogc2025_env_2025XXXX.yml`는 위 다운로드 파일의 이름으로 바꿔주세요!*)
    
    `conda env create -f ogc2025_env_2025XXXX.yml` 
    
4. 여러 패키지를 설치하는 과정이 보입니다. 모두 끝나면 다음을 실행합니다.
    
    `conda activate ogc2025`
    
5. Terminal 또는 cmd 창에 다음과 같이 표시되면 환경 설정 완료입니다.

![MacOS Terminal 예제.png](conda_terminal.png)



> 💡 Conda환경 설치 시 사용하는 HW 및 OS에 따라 오류가 발생할 수 있습니다. 특히, `pytorch`, `tensorflow`등에서 설치 문제가 발생할 수 있습니다. 이럴 경우 사용하지 않을 package들은 굳이 설치하지 않고도 알고리즘 개발이 가능합니다. 예를 들어, `pytorch` 설치 부분에서 계속 문제가 발생하고 참가팀이 `pytorch`를 사용하지 않는다면 제공되는 환경 설정 파일(`ogc2025_env_2025XXXX.yml`)에서 pytorch를 지우고 설치해 보세요. 문제가 지속되면 Slack 채널로 문의하세요.



> 💡 본 conda 환경은 참가팀의 알고리즘이 평가서버에서 문제 없이 실행될 확률을 높이기 위해 제공합니다. (제공하는 환경을 꼭 사용하지 않아도 평가 서버에서 잘 싱행 될 수도 있고, 제공하는 환경을 사용해도 OS등의 차이로 인해 다른 결과가 나올수도 있습니다.) conda 및 Python에 대한 일반적인 문제들은 참가팀들이 스스로 해결해야 하며 Slack 채널의 질문은 본 대회와 관련된 질문들로 한정합니다.


### 추가 설치된 Python 페키지 목록

아래 패키지들은 대회 시작 후 참가팀들의 요청으로 평가 서버에 추가 설치한 패키지들입니다. 참가팀들이 사용 가능합니다. (모든 참가팀이 사용할 필요가 없어 conda 환경 파일에 추가하지 않고 필요시만 별도로 설치할 수 있도록 목록을 제공합니다.)

```python
torch_cluster                1.6.3+pt26cu124
torch-geometric              2.6.1
torch_scatter                2.1.2+pt26cu124
torch_sparse                 0.6.18+pt26cu124
torch_spline_conv            1.2.2+pt26cu124
cdlib                        0.4.0
leidenalg                    0.10.2
stable_baselines3			 2.6.0
```

## 2. 알고리즘 제출 방법

[baseline_20250719.zip](baselines/baseline_20250719.zip)

[exercise_problems_20250512.zip](baselines/exercise_problems_20250512.zip)

먼저 위 두 파일을 받아서 적당한 폴더에서 압축을 해제합니다. `baseline_2025XXXX.zip` 은 알고리즘 예제 코드를 압축한 파일이고 `exercise_problems_2025XXXX.zip`은  예제 문제들을 압축한 파일입니다.

> 💡 `baseline_2025XXXX.zip` 과 `exercise_problems_2025XXXX.zip` 는 코드의 버그 수정등의 이유로 종종 업데이트 될 수 있음! 주기적으로 최신 버전을 확인 필요!


> 💡 `exercise_problems_2025XXXX.zip` 은 예선 **평가 문제**가 아니라 **연습 문제**임! 예선, 본선 등 각 단계의 평가 문제는 간 단계가 종료되면 공개하고 단계 진행중에는 공개하지 않습니다. 간 단계가 시작될 때 그 단계의 평가 문제들과 비슷한 유형의 문제들을 연습 문제를 공개합니다.


두 압축 파일을 다운로드한 후 적당한 폴더로 복사합니다.  

![Baseline Files](baseline_files.png)

그리고 다음과 같이 압축을 해제합니다.

![Baseline Files 압축 해제](baseline_files_decompressed.png)

`baseline` 폴더에는 다음의 파일들이 있습니다.

- `myalgorithm.py` : **실제로 알고리즘을 구현하는 파일. 가장 중요함!**
- `util.py`: 알고리즘 구현에 도움이 되는 함수들이 포함된 파일.

먼저 `myalgorithm.py` 파일을 살펴 봅시다. 

```python

import time
import util
import networkx as nx
from itertools import islice
import numpy as np

def algorithm(prob_info, timelimit=60):
    """
    This is a template for the custom algorithm.
    The algorithm should return a solution that is a list of (route, k) pairs.
    Each route is a list of node indices, and k is the index of the demand that is moved by the route.
    You CANNOT change or remove this function signature.
    But it is fine to define extra functions or mudules that are used in this function.
    """

    #------------- begin of custom algorithm code --------------#
    ...
    ...
    ...
    #------------- end of custom algorithm code --------------#

    return solution		
    
    ...

```

`algorithm()`함수는 다음의 파라미터로 호출됩니다.

- `prob_info` : 문제 데이터
- `timelimit` : 알고리즘 실행시간(초). 이 시간 이내에 알고리즘은 실행을 종료해야 합니다.

이 함수에 아래와 같은 부분들이 있습니다.

`#------------- begin of custom algorithm code --------------#`

⇒ 여러분이 작성할 알고리즘의 시작 부분을 나타냅니다. 이 줄 이후에 알고리즘을 작성하세요!

`#------------- end of custom algorithm code --------------#`
⇒ 알고리즘의 끝을 나타냅니다. 

위 두 줄 사이에 알고리즘 코드를 작성하면 됩니다. 이 때 주어진 `prob_info` 와 `timelimit` 파라메터를 사용하세요.

> 💡 `baseline` 코드는 아주 간단한 예제 알고리즘을 포함하고 있음. 실제 제출시에는 이 부분을 참가팀의 알고리즘 코드로 바꿔서 제출해야 함!


> 💡 `baseline` 코드를 일부 사용하거나 수정하는것은 허용되나 그대로 제출은 권장하지 않음! ⇒ 제출 가능한 횟수에는 제한이 있으니 의미없는 알고리즘 제출은 지양!


> 💡 `myalgorithm.py` 파일의 이름과 함수 `def algorithm(prob_info, timelimit=60):` 이름(function signature)은 **절대로** 변경 불가!



> `baseline` 에 포함된 `myalgorithm.py` 는 작성한 알고리즘의 간단한 테스트를 위한 코드를 포함하고 있음. `if **name** == "**main**":` 로 시작하는 부분의 코드를 참조할것.



`algorithm()` 함수로 제공되는 `prob_info`에서 문제의 데이터를 다음과 같이 가져옵니다.

```python
# Get parameters from the prob_info dictionary
N = prob_info['N']
E = prob_info['E']
K = prob_info['K']
P = prob_info['P']
F = prob_info['F']
```

- `N` : 노드 갯수. `0`은 gate 노드. `1 ~ N-1` 차량 적재 가능 노드
- `E` : Edge list. *Note: 방향이 없음(undirected)*
- `K` : 수요 list. `[ [[o,d)],quantyty], … ]`
- `P` : 항구 갯수. `0 ~ P-1` 의 순서로 항구 방문
- `F` : 경로 고정비

해는 다음의 구조를 가져야 합니다. 

- 주어진 문제에 대해  `항구번호: [(경로, 수요번호), (경로, 수요번호), …]` 의 형식의 Dict를 반환
    - 항구번호는 `0 ~ P-1` 의 숫자
    - 경로는 `[0,3,2]` 와 같이 노드의 목록으로 정의됨
    - 수요 번호는 K list의 index 값. `0 ~ len(K)-1` 의 값을 가져야 함
    - 예)
        
        ```python
        solution = {    
          0: [
            [[0, 1, 3, 5, 6, 9, 15, 20, 22], 2],
            [[0, 1, 2, 4, 7, 10, 14, 19, 21], 2],
            [[0, 1, 3, 5, 6, 9, 15, 20], 1],
            [[0, 1, 2, 4, 7, 10, 14, 19], 1],
            [[0, 1, 3, 5, 6, 9, 16], 1],
            [[0, 1, 3, 5, 6, 11, 17], 1],
            [[0, 1, 2, 4, 7, 12, 18], 1],
            [[0, 1, 3, 5, 6, 9, 15], 0],
            [[0, 1, 2, 4, 7, 10, 14], 0],
            [[0, 1, 3, 5, 8, 13], 0],
            [[0, 1, 3, 5, 6, 9], 0]
          ],
          1: [
            [[9, 6, 5, 3, 1, 0], 0],
            [[13, 8, 5, 3, 1, 0], 0],
            [[14, 10, 7, 4, 3, 1, 0], 0],
            [[15, 10, 7, 4, 3, 1, 0], 0],    
        	...
        }
        ```
        

`algorithm()`함수가 반환한 해의 목적함수 및 제약사항 만족 여부는 별도로 확인합니다. 자세한 사항은 문제 설명 페이지를 참조하세요.


> `util.py`파일의`check_feasibility(prob_info, solution)` 함수는 주어진 해의 유효성(feasibility) 여부와 목적함수를 계산합니다. 이 함수를 이용해서 해의 유효성을 꼭 확인하세요!


`check_feasibility(prob_info, solution)` 함수는 해의 유효성을 확인하고 다음과 같은 결과를 반환합니다.

```python
{
	'obj': 588.0,
	'feasible': True,
	'infeasibility': None,
	'solution': ...
}
```

위는 `checked_solution` 의 내용입니다. 중요한 항목을 살펴보면

- `obj` : 목적 함수(총 경로 비용에서 LB를 뺀 값)
- `feasible`: 해가 유효한지 여부. `True` or `False`
- `infeasibility`: 해가 유효하지 않은 이유. 해가 유효하면 `None`

> 💡 알고리즘의 실행 시간은 문제를 읽어오거나 `check_solution()` 등의 시간은 모두 제외하고 순수하게 `algorithm()` 이 실행되는 시간(wall clock기준)을 의미하고 이 시간이 제한시간을 넘으면 안됨! 제한시간을 넘으면 solution과 상관없이 penalty 점수를 얻게 되니 주의!


baseline 알고리즘은 문제 이해를 돕기위한 아주 간단한 알고리즘입니다. 중요한 두 가지 함수가 있습니다.

- `loading_heuristic(p, node_allocations, rehandling_demands)` :  항구 `p`에서 선적하는 휴리스틱입니다. `rehandling_demands`는 이 항구에서 하역 후 재적하는 수요들을 나타냅니다.
- `unloading_heuristic(p, node_allocations)` : 항구 p에서 하역하는 휴리스틱 함수입니다.

baseline 알고리즘 코드:

```python
# Loop over all ports and apply the loading and unloading heuristics to generate the routes
for p in range(P):

    print(f"Port {p} ==============================")

    if p > 0:
        # Unloading heuristic
        route_list_unload, rehandling_demands = unloading_heuristic(p, node_allocations)

        solution[p].extend(route_list_unload)
    else:
        rehandling_demands = []

    if p < P-1:
        # Loading heuristic
        route_list_load, node_allocations = loading_heuristic(p, node_allocations, rehandling_demands)

        solution[p].extend(route_list_load)

    print()
```

위와 같이 항구 방문 순서대로 하역→선적 휴리스틱을 반복합니다. 자세한 알고리즘 작동 원리는 baseline 코드의 함수들을 살펴보세요!


> baseline 알고리즘은 참가팀들의 이해를 돕고 새로운 알고리즘을 만드는 시간을 단축시키기 위해 제공됨. 새로운 알고리즘 개발할 때 참고하고 필요하면 코드를 사용해도 무방함!


### 알고리즘 제출 방법

작성한 알고리즘은 하나의 파일로 압축해서 제출합니다. 이 때 다음의 조건들을 만족해야 합니다.

- 하나의 `zip`파일로 압축 (압축 파일 이름은 임의로 지정)
- 필수 파일: `myalgorithm.py`, `util.py`
- `myalgorithm.py` 파일은 압축을 풀었을 때 하위 폴더에 있으면 안됨.
    - 압축 파일을 풀었을 때 최상위 폴더에 `myalgorithm.py`이 존재해야 함!


> 💡 **제출하는 압축파일의 `myalgorithm.py` 는 압축파일 내부의 하위 폴더에 있으면 안됨!!!!**
>
> **`myalgorithm_best.py`와 같이 파일 이름을 바꿔도 안됨!!!!**
>
> ![**올바른 제출용 압축 파일** (Windows)](zip_file_contents_win.png)
>
> ![*****잘못 압축된 제출용 파일***** (Windows)](zip_file_contents_win_err.png)
>
> MacOS 또는 Linux에서는 다음과 같이 압축파일 내부의 폴더를 조회할 수 있음
>
> ![압축 파일 내용 조회 (MacOS)](zip_file_contents_mac.png)
>
> 위와 같이 Terminal에서 `unzip -vl 압축파일.zip` 을 실행했을 때 **아래 경우와 같이 `myalgorithm.py`이 폴더(이 경우에는 `test1`) 아래에 위치하면 안됨!!!**


- 알고리즘 실행을 위해 필요한 추가적인 파일(neural network checkpoint 등)이 있으면 같이 압축함. 이 때 별도의 폴더를 만들어서 추가적인 파일들을 저장해도 문제 없음.
- `util.py` 파일은 수정 불가! (수정해서 같이 제출해도 평가시에 원래 파일로 replace한 후 평가함)
- 별도의 Python 파일(모듈)을 추가 가능. 모두 같이 압축되어야 함.
- 압축 파일의 크기는 **30MB이하** (용량이 부족하면 Slack 채널에서 용량 증가를 요청하세요.)

정리하면 압축을 풀어서 안에 포함된 `myalgorithm.py` 함수의 `algorithm()` 함수를 `ogc2025` 환경에서 실행이 가능해야 합니다. 

> 평가 서버에서는 **숨겨진** 문제로 알고리즘을 실행하기 때문에 문제 파일은 같이 제출할 필요 없음!

### Python이 아닌 다른 언어를 사용하는 방법

Python이 아닌 다른 언어 또는 라이브러리를 사용하는것도 가능합니다. 필요한 라이브러리를 모두 포함해서 압축 파일을 만들고 단지 `myalgorithm.py` 에서 다른 언어의 함수를 실행하도록 작성하면 됩니다. 

또한 참가팀들간의 형평성과 알고리즘 실행 환경의 변화를 방지하기 위해 전체 시스템에 설치가 필요한 언어 또는 라이브러리의 설치는 최대한 신중하게 결정할 예정입니다. 참가팀은 최선을 다해 사용 하고 싶은 모든것을 하나의 압축파일으로 localize 해서 다른 참가팀의 알고리즘 실행에 영향이 없도록 할 의무가 있습니다.

> 💡 Python이 아닌 다른 언어의 경우 평가 서버의 실행환경을 유의 할것. 예를 들어 C/C++로 알고리즘을 구현하고자 하면 먼저 Linux/Ubuntu 24.04 환경에서 컴파일 후 생성된 `so` 파일 또는 실행파일을 같이 압축하여 제출하여야 함. 평가 서버에서 컴파일 과정을 수행하지 않음!

> 💡 평가 서버와 같은 환경을 가상머신등을 통해 만들어서 알고리즘의 작동을 확인할것을 추천!

> 💡 별도의 컴파일 과정이 필요 없은 언어(Julia 등)를 사용하고자 할때도 Slack 채널을 통해 요청 후 사용 가능함을 먼저 확인할것!

> 💡 평가 서버에서 컴파일을 수행하지 않지만 작성한 라이브러리 등의 소스를 포함해서 압축 후 제출할것!


- Java
    - `sudo apt-get update`
    - `sudo apt-get install openjdk-17-jdk`
    - `java --version`
        
        ![Java 버전 확인](java_version.png)
        
- Dotnet framework(C#)
    - `sudo apt install -y dotnet-runtime-8.0`
    - `dotnet --info`
        
        ![Dotnet 버전 확인](dotnet_version.png)
        

### 알고리즘 제출 시 에러 코드 설명

알고리즘을 평가 서버로 제출하고 알고리즘의 실행이 끝나고 리더 보드가 업데이트 되면 참가팀은 제출한 알고리즘들의 실행 상태를 조회할 수 있습니다.위 그림과 같이 제출한 일자 별로 알고리즘의 실행결과를 보여줍니다. 알고리즘이 평가문제를 시간제한 내에 잘 풀면 화면과 같이 목적함수(평균 배달 비용)값을 보여줍니다. 만약에 알고리즘 실행도중에 문제가 발행하면 간단하게 그 이유를 알 수 있습니다. `alg_error` 는 알고리즘 실행 도중 `Python exception`이 발생했음을 의미합니다.  

![알고리즘 에러 코드 조회](error_code1.png)

![알고리즘 에러 코드 상세](error_code2.png)

- `infeasible` 은 알고리즘이 도출한 해가 유효하지 않음을 의미합니다.
- `time_limit` 는 알고리즘 실행 시간이 지정된 시간을 초과했을 경우에 발생합니다.
- `crash` 는 알고리즘이 실행 중 비정상적으로 종료(segmentation fault)된 경우를 의미합니다.
- `algo_error` 는 알고리즘 내부에서 에러가 발생하는 경우를 의미합니다.
- `test_error` 는 채점 과정에서 알고리즘을 실행이 불가능한 경우에 발생합니다. 
  - `test_error(01)`: 제출한 압축파일을 평가서버에서 압축해제 실패(잘못된 압축파일 형식)
  - `test_error(02)`: 제출한 압축파일의 최상의 폴더에서 `myalgorithm.py`를 찾을 수 없음
  - `test_error(03)`: 제출한 압축파일에서 `ogc2025` 환경에 존재하지 않는 Python package를 `import` 를 시도함

`sys_error`는 평가 시스템에서 발생하는 에러를 의미합니다. 

세부 에러코드는 대회가 진행됨에 따라 추가될 수 있습니다.

## 3. `alg_tester` 사용법

작성한 알고리즘을 테스트하고 제출하기 전에 제출형식(알고리즘 압축 등)을 간단하게 확인할 수 있는 방법을 소개합니다. 

[alg_tester_20250513.zip](baselines/alg_tester_20250513.zip)

위 파일을 다운로드한 후 적당한 폴더에서 압축 해제하세요. 압축 해제한 폴더에서 다음과 같이 `alg_tester.ipynb` 노트북을 불러옵니다.

![Algorithm Tester Files](alg_tester_files.png)

아래와 같은 노트북이 실행됩니다. 첫 번째 cell을 실행(`ctrl+enter`) 하세요.

![Algorithm Tester Notebook](alg_tester_notebook.png)

`alg_tester`는 다음과 같은 기능을 제공합니다.

- 제출할 알고리즘 압축파일을 읽어 압축형식 및 `algorithm()` 함수 존재 여부 확인
- 제출할 알고리즘을 여러 문제 파일을 대상으로 실행하여 알고리즘이 문제없이 해를 반환하는지 확인
- 문제의 해가 유효한 경우 목적함수를 계산하고 해를 시각화
- 여러개의 알고리즘과 문제의 조합으로 알고리즘을 실행하고 그 결과를 같이 비교할 수 있는 기능 등


> alg_tester 에서 작성한 일고리즘이 잘 실행 되더라도 평가 서버와와 환경 차이로 인해 평가 서버에서는 오류가 발생할 수 있습니다! 평가 서버의 환경은 문제설명 페이지를 참고하세요.
